// ============================================
// ============ Header Mobile Menu ============
// ============================================

// event.preventDefault();

$(() => {
  $('#hamburger').on('click', () => {
    $('#menu').slideToggle();
  });
});

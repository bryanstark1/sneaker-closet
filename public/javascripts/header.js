// ============================================
// ============ Header Mobile Menu ============
// ============================================

$(() => {
  $('#hamburger').on('click', () => {
    $('#menu').slideToggle();
  });
});
